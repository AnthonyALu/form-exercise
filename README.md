# Installation

In the project directory, please run:

### `npm install`

# Starting the project

In the project directory, you can run:

### `npm start`

# Test

In the project directory, you can run:

### `npm test`
